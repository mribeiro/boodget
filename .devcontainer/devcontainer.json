{
  "name": "boodget Dev",
  "dockerComposeFile": ["../docker-compose.yml", "../docker-compose.devcontainer.yml"],
  "service": "capital-tracker",
  "workspaceFolder": "/workspaces/money_manager",
  "overrideCommand": true,
  "forwardPorts": [3000, 5173],
  "postCreateCommand": "apk add --no-cache git && cd /workspaces/money_manager/backend && npm install && cd /workspaces/money_manager/frontend && npm install --include=dev",
  "postStartCommand": "(cd /workspaces/money_manager/backend && npm run dev) & (cd /workspaces/money_manager/frontend && npm run dev)",
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "anthropic.claude-code"
      ],
      "settings": {
        "editor.formatOnSave": true
      }
    }
  },
  "remoteEnv": {
    "SESSION_SECRET": "dev-only-secret-change-in-production",
    "DB_PATH": "/workspaces/money_manager/data/capital-tracker.db"
  }
}
